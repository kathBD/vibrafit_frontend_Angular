import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-catalog',
  standalone: true,
  imports: [CommonModule],
  template: `<p>Catálogo de Ejercicios — en construcción</p>`
})
export class CatalogComponent {}
